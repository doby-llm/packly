# Give the GitHub Actions Gradle daemon enough memory for Android debug APK assembly.
# The prior CI run used Gradle defaults (512 MiB heap / 384 MiB metaspace) and stopped due to GC thrashing.
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8 -XX:+UseParallelGC
